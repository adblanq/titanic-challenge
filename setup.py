from setuptools import find_packages, setup

setup(name="titanic_challenge",
      version="0.0.1",
      description="Kaggle titanic challenge",
      packages=find_packages(),
      include_package_data=True,
      zip_safe=False)
